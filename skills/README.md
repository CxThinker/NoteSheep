# Skills（技能）

共享 skills（技能）放在此目录下。

每个 skill 应使用以下结构：

```text
skill-name/
  SKILL.md
  agents/
    openai.yaml
  references/
  scripts/
  assets/
```

只有 `SKILL.md` 是必需文件。仅当可选目录能直接支持该 skill 时，才添加 `agents/`、`references/`、`scripts/` 或 `assets/`。
